# example-project
Project using CQRS, Clean Architecture, Repository Pattern, Specification Pattern


# fast startup
- execute the docker-compose file
    docker-compose up -d

- execute the script.sql file to ensure database and migrations are executed faster