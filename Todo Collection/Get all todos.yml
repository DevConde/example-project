info:
  name: Get all todos
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:5266/todo
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
